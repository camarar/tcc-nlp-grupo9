Testes passaram (2/2). Implementei `solucao.py` com `calcular_juros_compostos` seguindo as regras RN-01 a RN-10: conversão via `Decimal(str(valor))` com captura de exceções para `ValueError`, taxa mensal simples (`taxa_anual/12`), capitalização composta, validações de `principal`/`taxa_anual`/`meses` (incluindo `TypeError` fail-fast para `meses` não-int), e arredondamento `ROUND_HALF_EVEN` só no resultado final.
