Todos os cenários se comportam conforme especificado, e os 3 testes visíveis passam.

`solucao.py` implementado em `C:\Users\rapha\AppData\Local\Temp\tmpg4xstdym\solucao.py`, cobrindo:
- Validações em ordem (`NOTA_INVALIDA` → `ITEM_INVALIDO` → `REGIME_DESCONHECIDO` → `ALIQUOTA_NAO_CADASTRADA`), com `TABELA_INVALIDA` isolado nos métodos de cadastro.
- Resolução de alíquota exata/prefixo (≥4 dígitos) sem cruzar UFs; isenção só por match exato.
- ICMS quantizado por item (`ROUND_HALF_EVEN`), ICMS-ST com piso em zero e zerado incondicionalmente no SIMPLES; SIMPLES reduz ICMS à metade.
- Totais (`base_calculo`, `total_nota` com frete e sem ICMS próprio somado à parte) e `memoria_calculo` espelhando os mesmos erros/ordem de `calcular`.
- RNF-02 tratado explicitamente: conversões ambíguas/inválidas de entrada numérica viram erro de validação (documentado no docstring do módulo como decisão sob risco).


[duracao_s=74.2]